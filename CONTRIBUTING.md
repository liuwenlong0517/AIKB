# Contributing to AIKB

本文件定义什么样的信息值得进入 AIKB，以及信息进入正式知识区之前必须达到的质量标准。AIKB 的目标不是保存所有信息，而是保存未来仍能帮助开发者或 AI Agent 更快、更稳地做出正确判断和行动的信息。

## 核心判断

准备写入一条信息前，先回答：

> 当未来出现相同或相似情境时，这条信息能否减少重复调查、降低错误概率，或者帮助解释一个重要决策？

如果答案不明确，暂时不要写入正式知识区。

## 值得进入知识库的信息

满足以下至少一项的信息，才具有收录价值：

- 可复用的问题解决方案：问题可能再次发生，方案已经实际验证。
- 成本较高的调查结果：曾花费明显时间定位，未来重新调查的成本较高。
- 反直觉的约束或陷阱：仅靠常识容易做错，并且触发条件可以说明。
- 稳定的工程事实：对后续开发、调试、评审或发布有持续影响。
- 经过实践验证的工作流或检查清单：能够稳定改善效率、质量或可重复性。
- 重要决策及其理由：结论本身不足以解释为何这样选择，需要保留背景、权衡和边界。
- 跨项目可复用的工程知识：不依赖某个项目的偶然实现细节。
- 项目专属但长期有效的知识：未来参与该项目时必须知道，并且能够确认来源。
- 明确的个人工程偏好：会持续影响实现选择，同时写明理由、适用范围和例外情况。

## 正式收录的必要条件

进入 `knowledge/`、`experience/solutions/`、`experience/pitfalls/`、`experience/decisions/`、`workflows/` 或正式项目记忆的信息，必须同时满足：

1. **真实**：来自实际代码、文档、测试、故障处理或明确的个人决策，不是推测或生成式补全。
2. **可验证**：说明如何确认结论有效，并记录必要的验证结果。
3. **背景完整**：读者不依赖原始聊天记录也能理解问题为何出现。
4. **边界明确**：写明适用项目、技术栈、版本、环境、前置条件和已知例外。
5. **可行动**：能够指导判断、实施、排查或验证，而不只是抽象口号。
6. **可独立理解**：条目包含必要上下文，不依赖作者记忆或未链接的外部对话。
7. **可维护**：内容有合理的长期价值；容易过期的信息必须标明版本、日期或复核条件。

## 不应进入知识库的信息

以下内容不应直接进入正式知识区：

- 原始聊天记录、会议逐字稿或未经整理的终端输出。
- 临时想法、猜测、尚未验证的解决方案。
- 为了让目录看起来完整而编造的示例、经验或最佳实践。
- 没有背景和适用范围的代码片段、命令或结论。
- 只发生一次、无法复现且没有未来参考价值的偶然现象。
- 可以轻易从官方文档查到、但没有补充实践结论或使用边界的内容。
- 对外部文章或文档的大段复制；应提炼结论并保留来源链接。
- 与现有条目重复的信息；优先补充或修订已有条目。
- 已明确失效却没有历史决策价值的内容。
- 密码、访问令牌、私钥、个人敏感信息或其他不应长期保存的秘密。
- “保持代码整洁”“遵循最佳实践”等无法验证、无法执行的空泛表述。

## 候选信息与正式知识

尚未满足正式收录条件、但可能具有价值的信息，可以进入 `experience/inbox/`，并明确标记：

- 信息来源
- 当前假设
- 尚未完成的验证
- 预期适用范围
- 下一步处理动作

`experience/inbox/` 不是永久归档区。候选信息完成验证后应移入正式目录；确认无价值、重复或错误后应移除。

候选内容应使用 `templates/inbox-entry.md`。其 `status` 固定为 `candidate`，并记录捕获日期、来源类型、预期范围和下一步动作；候选内容不要求伪造 `last_verified` 等正式验证信息。

## 归档位置

- 通用工程原理、语言、框架和工具知识放入 `knowledge/`。
- 已验证的问题解决方案放入 `experience/solutions/`。
- 容易重复触发的错误、陷阱和规避方式放入 `experience/pitfalls/`。
- 包含背景、备选方案和取舍理由的决策放入 `experience/decisions/`。
- 可重复执行的开发、调试、评审和发布过程放入 `workflows/`。
- 仅对某个项目成立的事实和约定放入 `projects/<project>/`。
- 暂未验证或无法归类的候选信息放入 `experience/inbox/`。

## 条目结构

新增正式条目应使用 `templates/` 中最接近的模板，并至少包含：

- `背景`：背景和触发条件。
- `问题`：要解决的问题或需要保留的判断。
- `解决方案`：解决方案、结论或决策。
- `验证`：验证方式和结果。
- `适用范围`：适用范围、限制和例外。
- `关联信息`：来源、关联知识和后续阅读。

标题应能够被搜索，并直接表达问题或结论。一个条目只解决一个主要问题；主题过大时拆分并通过 `Related` 关联。

正式条目顶部使用统一的 YAML Front Matter，至少包含：

- `status`：正式有效条目使用 `verified`，已经失效但需要保留历史价值的条目使用 `deprecated`；`candidate` 只用于 `experience/inbox/`。
- `tags`：用于检索的技术和问题标签。
- `applicable_versions`：已确认适用的语言、框架、工具或项目版本；与版本无关时明确填写 `not-version-specific`。
- `last_verified`：最近一次验证日期，格式为 `YYYY-MM-DD`。
- `review_when`：需要重新检查该知识的触发条件，例如升级 Java LTS、Spring Boot 主版本或部署平台。
- `supersedes`：当前条目替代的旧条目路径；没有时使用空列表。

正式知识遵循“一条知识一个文件”。语言、框架、工具或工程主题使用目录组织，主题目录中的 `README.md` 只负责说明范围和导航，不集中堆放具体知识。文件名使用简短、可检索的英文小写短横线形式，例如 `virtual-thread-pinning.md`；正文说明使用中文，代码、命令、文件路径、API 和专有名词按实际需要保留英文。

## 索引维护

AIKB 使用两套职责不同的索引：

- 根目录 `INDEX.md` 面向 Agent，只提供稳定、轻量的分层入口，不登记全部具体知识。
- 根目录 `CATALOG.md` 面向用户，登记全部内容及其一句话说明，允许随知识库增长而扩展。
- 各目录的 `README.md` 是局部索引。分类索引指向主题，主题索引指向具体条目。

新增、移动、重命名或删除具体内容时，必须更新距离该内容最近的目录 `README.md` 和根目录 `CATALOG.md`。新增或删除主题时，还要更新上一级分类 `README.md`；只有基础文件、顶层分类或全局入口发生变化时才更新 `INDEX.md`。修改现有内容导致标题、状态、适用范围或索引摘要变化时，应同步更新局部索引和 `CATALOG.md`；仅修改正文细节时无需机械更新索引。

## 提交前检查

正式写入前逐项确认：

- [ ] 这条信息未来再次使用的可能性足够高。
- [ ] 它不是现有条目的重复内容。
- [ ] 结论已有实际证据或明确验证结果。
- [ ] 背景、触发条件和适用范围已经写清楚。
- [ ] 删除原始聊天记录后，条目仍可独立理解。
- [ ] 内容中没有密钥、隐私数据或不必要的敏感信息。
- [ ] 已选择正确目录，并按层级更新最近一级 `README.md`、`CATALOG.md`；只有全局入口变化时才更新 `INDEX.md`。

如果无法确认“验证结果”或“适用范围”，该信息只能进入 `experience/inbox/`，不能作为正式知识发布。

## AI Agent 写入规则

- 不要在每次任务结束后自动把对话写入 AIKB。
- 先从任务结果中识别候选知识，再按本文件判断是否值得收录。
- 不得把模型推断、未经执行的建议或未验证代码包装成既定经验。
- 写入前优先搜索现有条目；可以更新现有条目时，不创建重复文件。
- 修改既有结论时保留变更原因、验证依据和适用版本。
- 无法确认归档位置或证据充分性时，放入 `experience/inbox/` 或请求开发者判断。

## 维护与淘汰

知识不是永久正确的。发现条目过期、范围变化或结论被推翻时，应更新内容并说明验证依据；若旧结论具有决策历史价值，应标记为已废弃并指向替代条目，而不是让新旧结论同时保持有效状态。定期检查 `experience/inbox/`、版本相关知识和重复条目，避免知识库逐渐变成无法信任的信息堆积。
