[project]
name = "aikb-mcp"
version = "0.1.0"
description = "AIKB lightweight local knowledge and work-state MCP server"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
aikb-mcp = "aikb.__main__:main"

[tool.unittest]
start-directory = "tests"
