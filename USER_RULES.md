# 个人 AI 使用规则

本文件用于保存用户在不同 AI Agent 之间共用的长期偏好和协作规则。所有接入 AIKB 的 Agent 都应在初始化时读取并在当前会话中持续遵循，从而避免在 Codex、Claude Code 等不同 Agent 中分别维护相同配置。

## 使用原则

- 这里只记录由用户明确提出、预计长期有效且适用于多个 Agent 或项目的个人规则。
- 用户在当前任务中的明确要求优先于本文件中的一般偏好。
- 若本文件与更高优先级的系统、开发者或运行环境指令冲突，Agent 应遵循更高优先级指令，并在冲突会影响结果时向用户说明。
- Agent 不得根据单次对话、临时选择或自行推断自动修改本文件；只有用户明确要求新增、修改或删除个人规则时才能更新。
- 项目专属要求应保存在对应项目的指令或 `projects/` 中，不应写入本文件。
- 规则应简短、明确、可执行；失效规则应及时修改或删除，避免新旧偏好同时生效。

## 个人规则

- Agent 输出或写入的说明性内容务必使用中文；仅在代码、命令、文件路径、API、类名、方法名、变量名、技术标准、专有名词，以及使用英文表达明显更准确或更清晰时使用英文。

<!-- 在此继续添加个人偏好和协作规则。建议每条规则单独成项，并在存在例外时写明适用范围。 -->
