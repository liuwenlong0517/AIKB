# AIKB Agent 入口规则

AIKB 根目录由 Windows 用户环境变量 `AIKB_HOME` 指定；路径缺失或目标无效时，不猜测路径或扫描磁盘，提示用户运行 `system/tools/set-aikb-home.ps1` 并在设置后重启 Agent。下文路径均相对于该根目录。

每个新会话只在尚未加载时读取 `system/rules/USER_RULES.md`，并在本次会话中持续遵循。仅加载个人规则不代表已接入 AIKB，也无需反馈接入状态。根目录 `README.md` 是人类维护手册，不属于 Agent 默认上下文；仅在用户明确要求，或任务正在维护 AIKB 控制面、安装流程或文档时读取。

收到明确任务后再判断是否接入：软件开发、修改、调试、测试、评审、构建发布、工程设计、技术决策、项目知识查询及用户明确要求使用 AIKB 时接入；不依赖项目知识的一次性问答，以及写作、翻译、办公、媒体和生活类任务不接入。用户明确要求跳过时不接入；无法确定时暂缓，待任务需要时再接入。

任务首次需要接入时，读取 `system/rules/AI_RULES.md`，按其流程使用必要的知识、索引和工作状态，并简要反馈接入及实际加载的主题；已加载且未变化的个人规则不得重复读取。

本会话复用已加载规则。只在新会话、用户要求刷新、上下文丢失或文件已变化时重新读取本入口。
