# AIKB Agent 入口规则

AIKB 位于 `E:\CodeSpace\AIKB`。

每个新会话开始时，如果当前上下文尚未加载个人规则，则只读取 `E:\CodeSpace\AIKB\system\rules\USER_RULES.md` 并在本次会话中持续遵循。仅加载个人规则不代表已接入 AIKB，无需向用户反馈接入状态。

不要仅因打开新项目或新会话就自动接入 AIKB。收到明确任务后再判断：实际的软件开发、代码修改、调试、测试、评审、构建发布、工程设计与技术决策、项目知识查询，以及用户明确要求使用 AIKB 时需要接入；不依赖当前项目和 AIKB 的一次性问答或技术概念咨询，以及写作、翻译、摘要、办公、媒体处理、生活咨询等非编程任务不接入。用户明确要求跳过 AIKB 时不接入；无法确定是否有必要时默认暂缓接入。

任务首次满足接入条件且当前上下文尚未完成 AIKB 接入时，读取 `E:\CodeSpace\AIKB\system\rules\AI_RULES.md`，再按照其中的初始化流程读取索引和必要知识；`system/rules/USER_RULES.md` 已加载且未变化时不得重复读取。接入完成后向用户反馈已接入 AIKB。如果本次会话后续才出现需要接入的任务，则在该时点延迟接入。

完成接入后，在本次会话中持续遵循已加载的 AIKB 规则，并将已加载知识作为当前上下文记忆使用，不得在每轮对话中重复读取。仅在用户要求重载、上下文丢失、开启新会话或 `system/rules/AI_RULES.md` 规定的其他情况下重新加载。

对于已经接入 AIKB 的相关任务，按照 `system/rules/AI_RULES.md` 主动发现、验证并写入值得长期保留的工程知识。在符合写入规则的前提下，允许 Agent 直接新增或更新 AIKB，无需为每次常规知识写入单独请求确认；涉及敏感信息、争议结论或需要替用户作出新的长期决策时除外。

同一会话中不得仅为确认规则而重复读取本文件；用户明确要求刷新、上下文丢失、开启新会话或本文件已发生变化时重新读取。
