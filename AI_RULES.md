# AIKB Agent Rules

本规则适用于所有接入 AIKB 的 AI Agent，不绑定具体产品、模型或运行环境。具体 Agent 应在其根指令文件中配置 AIKB 的位置，并要求 Agent 按本文件完成接入。

## 1. 接入目标

AIKB 是 Agent 在不同项目中共享使用的长期工程知识层。Agent 接入后，应把已经加载的 AIKB 规则、索引和任务相关知识作为当前会话上下文的一部分，在后续对话中持续使用，而不是在每轮对话中重复读取。

AIKB 不替代项目代码、项目文档或 Agent 自身的根指令。发生冲突时，遵循当前运行环境的指令优先级，并明确指出冲突；不得静默混用互相矛盾的规则。

## 2. 首次接入流程

每个新项目或新会话首次接入 AIKB 时，按顺序执行：

1. 读取 `AI_RULES.md`，建立 AIKB 的使用、加载和写入约束。
2. 读取 `USER_RULES.md`，加载用户跨 Agent、跨项目共用的长期偏好和协作规则。
3. 读取 `INDEX.md`，建立知识库的轻量分层导航，不默认读取完整目录或扫描其他文件。
4. 根据当前任务，从 `INDEX.md` 进入相关分类，再沿目录中的 `README.md` 逐级查找并读取最少且直接相关的知识文件；若尚无明确任务，可以暂不加载具体知识。
5. 向用户明确反馈已接入 AIKB，并简要说明本次实际加载了哪些内容。建议使用：`已接入 AIKB，已加载基础规则、个人规则与索引。` 若同时加载了任务知识，应一并列出知识主题。

完成以上流程后，在当前会话中将 AIKB 标记为“已接入”。同一会话的后续对话应直接使用已加载内容，不得仅为确认规则而重复读取 `AI_RULES.md`、`USER_RULES.md`、`INDEX.md` 或已经加载且未发生变化的知识文件。

## 3. 重载条件

仅在以下情况重新读取基础规则、索引或相关知识：

- 用户明确要求重新加载、刷新或检查 AIKB。
- 开启了新的会话、新的 Agent 实例，或当前运行环境无法继承此前上下文。
- 上下文被压缩、截断、清空或丢失，Agent 无法可靠确认已加载的规则或知识内容。
- Agent 切换到另一个项目，且该项目的专属知识尚未加载。
- AIKB 文件在当前会话中被修改，相关旧内容可能已经失效；`USER_RULES.md` 被修改时应刷新个人规则。
- 当前任务进入此前未涉及的领域，需要按 `INDEX.md` 增量加载新的知识文件。
- Agent 对已加载内容的准确性没有把握，继续使用可能造成错误。

重载应遵循最小化原则：只刷新可能失效或当前任务新增需要的内容。除非用户明确要求全面检查，否则不得重新扫描整个 AIKB。因上下文丢失而重新接入时，应再次向用户反馈；普通的增量加载无需重复宣告“已接入”，只需在相关时说明新增加载的知识。

## 4. 知识加载原则

- 禁止在没有任务需要时默认读取整个 AIKB。
- 优先读取最具体、最接近当前问题范围的文件。
- 当通用知识与项目级事实都相关时，只加载对应的通用条目和项目条目。
- 已加载知识在当前会话中视为可复用上下文，不因对话轮次增加而重复读取。
- `USER_RULES.md` 中的个人偏好在当前会话中持续生效，但用户针对当前任务提出的明确要求优先。
- 新任务涉及不同技术领域时，通过 `INDEX.md` 和对应目录的 `README.md` 增量加载，不重新执行完整初始化。
- `CATALOG.md` 是面向用户的完整内容目录，可能随知识增长而变大。常规任务不默认读取，仅在用户要求浏览全部内容、需要全库查找或维护知识时按需加载。
- 若没有相关知识，应明确说明 AIKB 中未找到，不得把模型自身知识描述成 AIKB 已有结论。
- 使用知识时必须保留其验证条件、版本和适用范围，不得把局部经验扩展为普遍规则。
- 若 AIKB 内容与当前代码、测试结果或权威来源不一致，应以可验证的当前证据为准，并把差异作为修订候选处理。

## 5. 主动发现知识

Agent 在开发、调试、评审、发布和技术决策过程中，应主动识别值得长期保留的信息，而不是等待用户逐条要求记录。尤其关注：

- 已实际解决且可能再次发生的问题，以及能够稳定复现的解决步骤。
- 调查成本较高的根因、诊断路径、关键日志特征或排除方法。
- 反直觉的行为、容易重复触发的陷阱、隐含前置条件和规避方式。
- 经过代码、测试、文档或运行结果确认的稳定工程事实。
- 能够重复执行并稳定改善结果的开发、调试、评审或发布流程。
- 对未来实现有持续影响的重要决策、备选方案、取舍理由和退出条件。
- 可跨项目复用的语言、框架、工具和工程实践知识。
- 仅对当前项目成立、但后续参与者必须知道的长期项目事实。
- 用户明确表达且预计长期有效的工程偏好、约束和例外。

以下情况通常不构成知识：容易从官方文档直接查询且没有实践增量的信息、一次性操作过程、未经验证的模型建议、原始聊天记录、无上下文的命令输出，以及仅为当前临时任务服务的实现细节。

## 6. 主动写入流程

Agent 发现候选知识后，应在不打断当前主要任务的前提下完成以下流程：

1. **捕获候选**：记录候选结论、产生背景、证据来源和可能适用范围，避免任务结束后只剩模糊印象。
2. **搜索去重**：通过 `CATALOG.md`、相关主题索引和关键词检查现有条目。已有相关条目时优先补充、修订或关联，不创建内容重复的新文件。
3. **判断价值**：确认该信息未来能够减少重复调查、降低错误概率、提升执行一致性，或解释重要决策。
4. **验证结论**：优先使用实际代码、自动化测试、可复现命令、运行结果、权威文档或用户明确确认作为证据。Agent 自身推断不能作为唯一验证依据。
5. **选择归档位置**：按照 `CONTRIBUTING.md` 将内容放入 `knowledge/`、`experience/`、`workflows/` 或 `projects/` 的正确位置。
6. **结构化写入**：使用最接近的模板，写清“背景”“问题”“解决方案”“验证”“适用范围”和“关联信息”。标题应能直接检索，一个条目只表达一个主要结论。
7. **维护导航**：新增、移动、重命名或删除具体内容后，更新距离该内容最近的目录 `README.md`，并同步更新根目录 `CATALOG.md`。只有基础文件、顶层分类或全局入口发生变化时才更新根目录 `INDEX.md`。仅修改正文且索引摘要、状态和适用范围均未变化时，无需机械修改索引。
8. **反馈结果**：在当前任务的阶段性或最终反馈中，简要说明新增或更新了哪些知识、保存位置以及验证依据，不粘贴整篇内容。

对于已经达到正式准入条件、归档位置明确且写入不会改变用户业务代码的知识，Agent 应主动完成写入，无需等待用户再次提醒。若信息有潜在价值但证据不足、归类不确定或范围尚未确认，则写入 `experience/inbox/`，明确标记来源、当前假设、待验证项、预期范围和下一步动作，不得包装为正式结论。

当写入涉及敏感信息、可能泄露秘密、会覆盖存在争议的既有结论，或需要替用户作出新的长期工程决策时，Agent 必须暂停写入并请求用户确认。不得记录密码、令牌、私钥、个人隐私或其他不应长期保存的数据。

## 7. 写入时机与任务优先级

- 主要任务优先。知识整理不得阻塞故障恢复、关键验证或用户当前明确要求的交付。
- 候选知识应在相关证据仍可获得时记录，正式写入通常在结论验证完成后或任务收尾阶段进行。
- 长任务中若已形成独立、稳定且高价值的结论，可以阶段性写入，避免上下文丢失。
- 同一任务产生多个候选时应合并去重，只保留具有长期价值的最小知识集合。
- 写入完成后应检查条目能否脱离当前对话独立理解，并确认没有把推测写成事实。

## 8. 质量与维护

准备写入、修订、归档或淘汰知识前，应读取 `CONTRIBUTING.md`；如果当前会话已加载且文件没有变化，可以直接沿用，不必重复读取。

发现既有知识过期、范围变化、证据被推翻或与当前事实冲突时，Agent 应主动修订，并记录变化原因、验证依据、适用版本或复核日期。旧结论具有决策历史价值时，应标记为已废弃并指向替代条目；没有保留价值时再删除，避免互相冲突的结论同时保持有效。

## 9. 内容边界

- AIKB 是共享知识层，不是具体 Agent 的配置目录。
- 不在 AIKB 内创建 `AGENTS.md`、`CLAUDE.md` 或其他 Agent 专属根指令文件；接入导向规则应配置在各 Agent 或具体项目之外的相应指令位置。
- 不因维护 AIKB 而擅自修改外部项目代码、项目文档或项目根目录配置。
- 项目专属事实与通用工程知识必须分开维护。
- 不把整个对话、完整终端日志或任务流水账写入 AIKB，只提炼得到证据支持的长期结论。
- 不得把 Agent 推断出的用户偏好自动写入 `USER_RULES.md`；该文件只能根据用户的明确指令更新。
